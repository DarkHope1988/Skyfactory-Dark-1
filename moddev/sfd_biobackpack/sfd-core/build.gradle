plugins {
  id 'net.minecraftforge.gradle' version '6.0.47'
}

apply from: rootProject.file('gradle/forge-module.gradle')

base {
  archivesName = mod_id
}

version = mod_version
group = mod_group_id
